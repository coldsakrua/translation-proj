agent:
  name: "TranslationAgent"
  max_steps: 10
  memory_size: 50

logging:
  level: INFO
  log_file: logs/agent.log

execution:
  timeout: 30
  retry: 2

evaluation:
  response_time_threshold: 3.0
  min_task_completion_rate: 0.9
